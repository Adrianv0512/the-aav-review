---
import { settings } from "../settings";
import { url } from "../utils/urls";
const path = Astro.url.pathname;
---

<header class="site-header wrap">
  <a class="wordmark" href={url()} aria-label={`${settings.title} — home`}>
    <span class="brand-logo" aria-hidden="true">
      <img
        src={url("brand/mountain-logo.jpg")}
        alt=""
        width="600"
        height="600"
      />
    </span>
    <span>{settings.title}</span>
  </a>
  <nav aria-label="Main navigation">
    <a
      href={url("writing/")}
      aria-current={
        path.includes("/writing") || path.includes("/tags") ? "page" : undefined
      }
    >
      Articles
    </a>
    <a
      href={url("about/")}
      aria-current={path.includes("/about") ? "page" : undefined}
    >
      About
    </a>
    <a class="rss-nav" href={url("rss.xml")} aria-label="RSS feed">
      <svg
        width="19"
        height="19"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="1.8"
        aria-hidden="true"
      >
        <circle cx="5" cy="19" r="1" fill="currentColor" />
        <path d="M4 11a9 9 0 0 1 9 9M4 4a16 16 0 0 1 16 16" />
      </svg>
    </a>
  </nav>
</header>
